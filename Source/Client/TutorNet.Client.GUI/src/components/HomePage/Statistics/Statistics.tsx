import { styled } from "@mui/material";

const Container = styled("div")({
    minHeight: "580px"
  });

export default function Statistics(){
    return(
        <Container>
            
        </Container>
    );
}